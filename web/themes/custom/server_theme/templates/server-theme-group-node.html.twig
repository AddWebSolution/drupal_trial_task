<div class="flex flex-col md:flex-row gap-y-5 sm:gap-x-10 md:gap-x-14">
  {% if flag_message %}
    <div class="w-full max-w-3xl">
      Hi {{ name }}, <a href="/group/node/{{ entity_id }}/subscribe">click here</a> if you would like to subscribe to this group called {{ label }}.
    </div>
  {% endif %}
</div>
